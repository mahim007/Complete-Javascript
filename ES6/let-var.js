function drivingLicense5(isPassed) {
    if(isPassed) {
        var firstName = 'mahim';
        var yearOfBirth = 1995;
    }

    console.log(firstName + " born in " + yearOfBirth);
}

drivingLicense5(true);

function drivingLicense6(isPassed) {
    if(isPassed) {
        let firstName = 'mahim';
        const yearOfBirth = 1995;
    }

    console.log(firstName + " born in " + yearOfBirth);
}

drivingLicense6(true);